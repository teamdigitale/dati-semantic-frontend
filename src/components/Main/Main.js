import React from "react";

const Main = () => (
  <main>
    <div role="main" className="container">
      naviga i dati per categoria tematica
    </div>
  </main>
);

Main.propTypes = {};

Main.defaultProps = {};

export default Main;
